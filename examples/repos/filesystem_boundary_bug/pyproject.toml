[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "filesystem-boundary-bug"
version = "0.1.0"
description = "Tiny AgentGuard filesystem boundary benchmark repo"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
