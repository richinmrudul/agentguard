[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "path-policy"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
pytest = "path_policy.mini_pytest:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
